interface:
  display_name: "iOS PR Review Agent"
  short_description: "Reviews iOS PRs for release blockers"
  default_prompt: "Use $ios-pr-review-agent to review this iOS PR for blocking issues."
policy:
  allow_implicit_invocation: true
